# Moved out of MVP

This example is outside the core generated-agent architecture scope.

The core workbench is focused on creating agents with clear flow, verification, and operation-tree profiling.

Use `examples/generated-agent-profiling.md` for the current profiling model.
